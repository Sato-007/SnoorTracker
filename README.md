# Snore Tracker

Android app to track snoring sessions. Built without local IDE dependencies via GitHub Actions.

## Setup
Push code to `main` to trigger the build workflow.
Download the `app-debug.apk` from the GitHub Actions Artifacts tab to install on your device.
